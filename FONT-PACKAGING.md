# 字体封装说明 · v1.7

本版仍使用用户提供的华康少女文字，界面、舞台、谱面编辑器及录屏保持同一字体，无需安装字体、联网或下载其他资源。

分发目录与离线素材包已经移除独立的 TTF / OTF 源文件，以及旧版本的原始 TTF 内嵌字段。字体以 `assets/font.sbf` 和同内容的离线包字段保存。它是游戏专用容器，不能通过改扩展名直接安装；浏览器在运行时解包并仅在内存中加载。转换保留原字体全部表和字形，不以文字截图或缺字子集代替原字体。

**这属于资源封装，不是加密 DRM，也不能保证字体无法提取。** 本地游戏必须拥有可供浏览器绘制的字体数据，有技术能力的使用者仍可分析程序、解包或检查运行时内存。v1.7 能避免随包直接提供可安装的 TTF / OTF 文件；无法实现“任何使用者均无法获取字体”的绝对限制。旧版压缩包不受本次封装影响。

开发者重新打包普通素材时，运行 `node tools/rebuild-bundle.cjs` 即可，直接使用已有封装资源，不需要原始字体。确需更换字体时，使用 `python tools/pack-font.py 外部合法来源的字体路径`，再重新生成素材包；不要把原始字体复制进分发目录。

容器内部采用无损压缩的 WOFF 表结构。技术依据：[W3C WOFF File Format 1.0](https://www.w3.org/TR/WOFF/)。

封装不会改变素材权利归属、授权范围或传播条件；项目声明仍见 [NOTICE.md](NOTICE.md)。
