# v1.9 验证记录

日期：2026-09-21。环境：Windows、Edge / Chromium、Playwright 触屏模拟。

## 已完成

- 节奏引擎 16 项与齿轮 / 图案规则 13 项回归通过，另验证教程、结算语音、350 事件练习曲及乐句配色。
- 三首标准谱面的事件数、原时间证据、长按 / 齿轮时刻、手动输入及自动演示检查通过；与 v1.8 JSON 保持一致。
- 三首简单谱面分别为 212 / 148 / 245 个事件。逐个核对为标准版原事件子集；时间、尾点、轨道和颜色一致；完整手动输入回放均为零 MISS。
- 完整版主页 BGM 开 / 关按钮、两个复选框同步、刷新后保留关闭状态、开局即停、返回即按设置恢复通过。
- 模拟浏览器拒绝自动播放，验证可见提示及用户点击页面后恢复；不绕过浏览器播放限制。
- 三首简单 / 标准互切、内置音乐加载、包含音乐的简单谱面 JSON 导出通过。
- 三首标准曲的实际浏览器选曲 / 播放 / 重来 / 导出 / 导入，以及 MP4 录制、下载通过。
- 原有角色输入 8 组浏览器回归通过：先按决定高度，后按触发交叉，MISS 不自动换行，暂停 / 重来清除输入。
- 轻量版 320 / 360 / 390 / 768 px 的主页、设置、工坊没有横向溢出。
- 真实浏览器触摸事件验证：短按、长按、双指下劈 / 上撩、下劈期间避开齿轮、滑出后继续长按、松手释放、取消触摸、暂停清空输入、键盘与触屏共同持有同一键。
- 包含预期 GOOD 与 MISS 的触屏样例结果为 8 PERFECT / 1 GOOD / 1 MISS，2 个齿轮均成功避开。
- 390 × 844 与 844 × 390 全屏下，触控中心相对画面判定圈误差小于 1 CSS px；全屏暂停、恢复与页面全屏回退通过。
- 轻量版没有换素材对话框、图片上传入口、录制控件、录制模块、主页音乐控件或主页音频数据。
- 本轮浏览器测试无 JavaScript 页面错误，标准谱面回归无外部网络依赖。

## 实际测试范围

移动端检查使用 Chromium 触屏模拟和横竖屏尺寸切换，不等同于实体手机测试；未在实体 iOS Safari 上验证。手机访问应使用静态网站地址，系统文件预览器可能禁止脚本。

游戏中播放的音频保持 v1.8 原字节内容，未重新压缩或移动起音。轻量版只缩小人物和装饰贴图并采用无损 WebP 编码；字体继续封装。压缩包体积、CRC、资源哈希和解压后启动另由打包检查核验。
