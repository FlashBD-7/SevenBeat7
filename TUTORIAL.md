# 新手教程 · 第一颗星

60 秒，固定 120 BPM。包含 22 个短按、3 个长按、5 个齿轮，共 30 个事件。配乐为本 Demo 程序合成的原创练习音乐，使用钟琴式旋律、和弦、低音与鼓点，没有使用第三方歌曲或音色采样。教程事件位于配乐的拍点上，难度刻意放低。

## 学习顺序

| 时间 | 练习 | 操作（默认键位） |
| --- | --- | --- |
| 0–4 秒 | 认识按键和节拍 | D 为空中，J 为地面，等怪兽抵达光圈 |
| 4–12 秒 | 地面短按与判定 | 6–11 秒的怪兽每次轻按 J 并松开 |
| 12–20 秒 | 空中短按 | 14–19 秒的怪兽每次轻按 D 并松开 |
| 20–28 秒 | 两轨长按 | J 从 22 按到 24 秒；D 从 25 按到 27 秒 |
| 28–35 秒 | 跳起躲齿轮 | 松开 J，提前按住 D，避开 31、33 秒的齿轮 |
| 35–44 秒 | 空中下劈 | 继续按住 D；38、40、42 秒轻按 J，下劈与齿轮同时出现 |
| 44–52 秒 | 地面上撩 | 先松开双键；J 从 46 按到 51 秒；47、49、50.5 秒轻按 D |
| 52–58 秒 | 换手练习 | 松开双键，54–57 秒依次按 J、D、J、D |
| 58–60 秒 | 结束提示 | ESC 暂停，R 重来；设置可改键和校准延迟 |

实际游戏提示会跟随自定义键位更新。暂停时提示停留在当前步骤，重来会从头开始；从工坊游标练习时会显示对应时间段的提示。没有输入也会继续播放到结束并显示漏击，可重来练习。教程不会强制替玩家按键；只有勾选「自动演示」时才自动游玩。

## 两种姿势的规则

先按住 D 再按 J，角色仍在空中，显示「下劈」并判定地面音符；松开 J 立即解除下劈。先按住 J 再按 D，角色仍在地面，显示「上撩」并判定空中音符；松开 D 立即解除上撩。按住两键较长时间时，同样维持这套规则，能同时完成两轨长按。

想改变高度，应松开先按住的键。地面上撩不是跳跃，遇到齿轮仍会碰撞；空中下劈保持空中，因此仍能躲开。长按是否命中和保持由各轨按键独立判定，姿势切换不会自动帮玩家完成另一轨的音符。

人物高度和姿势不会由教程提示或谱面自动改变。示例中第 38、40、42 秒的地面短按与齿轮故意同时出现，用于练习新的安全下劈。

## 素材和修改

- `assets/character-upslash.png`：上传的「上撩 (1).png」原始副本。
- `assets/character-downslash.png`：上传的「下劈.png」原始副本。
- 新动作仅在运行时去除连通白底，未修改原文件；下劈原图朝右，默认显示不翻转。上撩 / 下劈可在外观设置中独立替换。
- `charts/tutorial.json` 保存音符与 `source.lessons` 提示；可以在工坊导出包含音乐的便携 JSON。修改音符不会自动重排提示时间，若改变教程结构应同时编辑 `source.lessons`。
- `tools/make-tutorial.py` 可用 Python + numpy 重建原始 WAV 与教程 JSON，之后用 AAC 编码器生成 `assets/tutorial.m4a`，再运行 `node tools/rebuild-bundle.cjs` 更新离线包。普通游玩不需要这些开发工具。

首次打开且没有已保存工程时默认选中教程；已有工程继续恢复，也可以点击首页教程横幅随时载入。v1.8 曲库包含本教程、三首视频整理谱面和原创曲《星糖放学后》；早期两个测试谱面已移除，声明继续保留。
