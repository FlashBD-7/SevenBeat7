# 三首视频参考谱面 · v1.9 标准与简单模式

三份谱面最初来自用户提供的三个视频。本版按新的练习手感要求整理连续短按，保留视频音乐、长按首尾与齿轮时间；已经不再逐帧照搬原短按时间。原视频识别依据仍保留在 source 中，可与整理后的时间核对。

## 曲目与来源

| 曲目 | 上传的视频 | JSON 文件 |
| --- | --- | --- |
| MEGALOVANIA | 【喵斯快跑 Muse Dash】UNDERTALE　Megalovania - Toby Fox.mp4 | charts/megalovania.json |
| Unwelcome School | 【Muse Dash x 蔚蓝档案】银行不妙曲 Unwelcome School 10级 AP.mp4 | charts/unwelcome.json |
| ぱぴぷぴぷぴぱ · AP | 【Muse Dush】ぱぴぷぴぷぴぱ（papipupipa) 大触10级 AP.mp4 | charts/papipupipa_ap.json |

| 曲目 | 编辑网格 BPM | 短按 | 长按 | 齿轮 | 总事件 | 音轨时长 |
| --- | ---: | ---: | ---: | ---: | ---: | --- |
| MEGALOVANIA | 120 | 621 | 50 | 20 | 691 | 156.39 秒 |
| Unwelcome School | 180 | 560 | 72 | 42 | 674 | 127.75 秒 |
| ぱぴぷぴぷぴぱ · AP | 200 | 895 | 28 | 38 | 961 | 178.80 秒 |

面板的「击打」包含短按和长按，不包含齿轮；长按只计一个事件。BPM 用于工坊网格，实际击打仍由 JSON 中的秒数决定。本版使用局部节拍网格校正连续短按，并移除孤立的过密点；不会把真正的休止和所有节奏型强行变成同一种间隔。

## 标准版沿用的 v1.8 调整

| 谱面 | 移除过密短按 | 短按最大移动 | 同轨短按最短间隔（之前 → 现在） |
| --- | ---: | ---: | --- |
| MEGALOVANIA | 6 | 25.0 ms | 33.3 → 125.0 ms |
| Unwelcome School | 95 | 33.334 ms | 33.3 → 166.666 ms |
| ぱぴぷぴぷぴぱ · AP | 123 | 33.334 ms | 33.3 → 141.666 ms |

按四小节估计局部拍点，在合理范围内校正短按的识别抖动；八小节内每条轨道保持固定配色，乐句之间换色。多数短按已落在该段均匀网格上，少量偏离网格的原始节奏保留。长按起点、持续时间、齿轮时刻及三个 M4A 音频均未修改。

`source.noteEvidence.videoTime` 是原视频时间，`chartTime` 是当前谱面时间，`timingShiftMs` 是调整量；`source.regularization` 保存段落网格与移除原因。汇总另见 [CHART-REVISION.json](CHART-REVISION.json)。手工修改后，这些历史依据不会自动重新测量。

## 最初的视频重建依据

- 三个源视频都是 60 帧每秒。普通判定以可见的连击数字变化帧为主要时间依据，同时用两条不同位置的扫描线核对音符轨道和运动速度。数字开始变化但暂未完整显示的帧进行回查。
- 长条结合可见轨道条带与首尾判定确定。低连击尚未显示数字的开头、遮挡、隐形音符及疑似长条片段另作画面复查。开头的个别时刻采用两帧之间的估计值。
- v1.5–v1.7 的普通音符保留视频时钟；v1.8 短按时间按上文整理。所有谱面 `offset` 仍为 0，原视频开头仍保留。
- 齿轮使用两条扫描线之间的运动时间估算其到达角色脚下的时刻，独立于普通怪兽的击打光圈。保持空中即可躲避，地面攻击不会击毁它。
- 每个事件在 `source.noteEvidence` 保留视频时间、帧号、方法；普通事件另存连击变化与扫描强度，长按另存尾帧，齿轮另存运动扫描数据。画面识别的帧粒度约 16.7 ms，**这不代表每个事件的还原误差一定小于 16.7 ms**；特效遮挡、动画延迟和轨道判断仍可能带来误差。
- 三个 M4A 直接复制视频内的 AAC 音轨，没有变速或重新编码，也没有裁掉开头。Unwelcome School 与 AP 视频仅截去关卡后的结算尾段。视频原本录入的音效会继续存在，无法通过游戏音效滑块消除。

## 特殊机制的适配

当前游戏支持短按、长按、地面齿轮三种事件；原作的重击采用一次普通按键，Boss / 特殊敌人使用本游戏怪兽外观表现。同一时刻清楚可见的上下轨音符仍保留为双轨输入。

原作连续敲击的鼓点袋转换为持续长按，共 6 段：

| 曲目 | 起点（视频秒） | 所在轨道 |
| --- | ---: | --- |
| Unwelcome School | 28.800 | 地面 |
| Unwelcome School | 39.467 | 地面 |
| Unwelcome School | 82.133 | 地面 |
| Unwelcome School | 103.467 | 地面 |
| ぱぴぷぴぷぴぱ · AP | 51.533 | 地面 |
| ぱぴぷぴぷぴぱ · AP | 137.350 | 地面 |

这些长按从画面复查的首次击打附近开始，到最后可见判定结束；它们不要求复现原作每一次连敲。原作重击、长条首尾、多次连打与本游戏的计分规则不同，因此原视频的 731 / 868 / 1269 连击不能直接作为本游戏音符总数的核对目标。原作奖励物与完整 Fever / Boss 机制未加入。

## 游玩与微调

1. 解压完整文件夹，用 Edge / Chrome 打开 `index.html`，或双击 `START.cmd`。
2. 在首页点三张歌曲卡片之一，等待载入后开始。默认 D 为空中、J 为地面；按住 D 可持续跳起躲避齿轮，人物位置仍由按键决定。
3. 需要先看整体节奏时勾选「自动演示」。它会模拟输入，不能作为手动成绩。
4. 点「在谱面工坊中打开」，使用试听和「从游标练习」检查某一段；吸附选「自由」可保留视频时间精度。更改 BPM 不会自动挪动已有音符。
5. 工坊勾选「包含音乐」再导出，即得到可单文件携带的 JSON。直接导入包内不含音乐的 `charts/*.json` 时，请另外导入同名 `assets/*.m4a`；用内置曲库切换则会自动配好音乐。

若整体总是偏早或偏晚，先调整个人的击打延迟。若只有某一处与视频不同，直接修改该音符的时间、轨道或时长；原始识别依据会保留在 `source` 中作为参考，不会随手工编辑自动重新测量。

## v1.9 简单模式

| 曲目 | 简单版文件 | 短按 | 长按 | 齿轮 | 总事件 |
| --- | --- | ---: | ---: | ---: | ---: |
| MEGALOVANIA | charts/megalovania_easy.json | 203 | 6 | 3 | 212 |
| Unwelcome School | charts/unwelcome_easy.json | 142 | 0 | 6 | 148 |
| ぱぴぷぴぷぴぱ | charts/papipupipa_ap_easy.json | 235 | 3 | 7 | 245 |

简单模式以标准版为基础降低密度，保留乐句内稳定的单一脉冲，不新增或移动事件。音频、保留音符的时间、轨道、颜色和长按持续时间均与标准版相同。击打之间至少留约 0.38 秒，长按结束后有空隙，齿轮前后至少约 0.85 秒无其他击打。相邻小段可能因原曲休止或节奏型而有变化。

不要求新手双押或一边长按一边切换另一行。简单版的 source.parentChart 指向标准版，source.noteEvidence 仅收录保留的事件，方便追溯。内置音乐由简单与标准版本共用；JSON 导出可勾选「包含音乐」。轻量版的音频全部在 assets/bundle.js 中，没有另存重复音频文件。
